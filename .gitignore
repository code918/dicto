# ⚠️ 공개 저장소입니다. 개인 설정·비밀·사용자 데이터는 절대 올리지 않습니다.
# 새 종류의 로컬 파일이 생기면 커밋 전에 여기부터 추가하세요.

# ── 비밀 / 인증 ─────────────────────────────────────────────
.env
.env.*
!.env.example
*.secret
secrets.*
credentials*.json
service-account*.json
GoogleService-Info.plist
*.pem
*.key
*.p8
*.p12
*.pfx
*.cer
*.certSigningRequest
*.keychain
*.keychain-db
*.mobileprovision
*.provisionprofile
AuthKey_*
id_rsa*
id_ed25519*
*.ppk

# ── 개인 로컬 설정 ──────────────────────────────────────────
# 서명 팀 ID 등 (Makefile이 읽음)
local.mk
*.local
*.xcconfig.local
Local.xcconfig

# AI 코딩 도구 설정·메모 (개인 경로, 토큰, 작업 기록이 들어갈 수 있음)
.claude/
CLAUDE.md
CLAUDE.local.md
AGENTS.md
.mcp.json
.cursor/
.codex/
.gstack/
.context/
.aider*

# 편집기
.vscode/
.idea/
*.swp
*.swo
*~

# ── 앱 사용자 데이터 (받아쓰기 기록, 개인 사전, 모델) ───────
*.log
history.log
dictionary.txt
learned.txt
claude-work/
models/
*.bin
.relaunch
.gpu-loading

# ── Xcode / 빌드 산출물 ─────────────────────────────────────
build/
DerivedData/
.build/
.swiftpm/
# XcodeGen이 project.yml에서 생성
*.xcodeproj
*.xcworkspace
xcuserdata/
*.xcuserstate
*.xcscmblueprint
*.moved-aside
*.app
*.dSYM
*.dSYM.zip
*.xcarchive
*.ipa
*.dmg
*.pkg
*.crash
*.ips

# ── macOS ──────────────────────────────────────────────────
.DS_Store
._*
.AppleDouble
.LSOverride
.Spotlight-V100
.Trashes
.fseventsd
Icon?
